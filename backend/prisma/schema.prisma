/// Prisma schema placeholder - Phase 1 setup
///
/// Models and enums will be implemented in Phase 3 (T018-T040).

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
