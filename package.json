{
  "name": "yildiz-bayan-kuaforu",
  "version": "0.1.0",
  "private": true,
  "description": "Appointment & Customer Management Suite for Yıldız Bayan Kuaförü",
  "author": "Yıldız Bayan Kuaförü Team",
  "license": "UNLICENSED",
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\"",
    "build": "pnpm --filter backend build && pnpm --filter frontend build",
    "lint": "pnpm --filter backend lint && pnpm --filter frontend lint",
    "test": "pnpm --filter backend test && pnpm --filter frontend test",
    "clean": "rm -rf backend/dist frontend/.next backend/node_modules frontend/node_modules node_modules"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  }
}
